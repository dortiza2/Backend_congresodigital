{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Postgres": "Host=${DB_HOST};Port=${DB_PORT:-5432};Database=${DB_NAME:-congreso};Username=${DB_USER};Password=${DB_PASSWORD};Ssl Mode=Require;Trust Server Certificate=true;Pooling=true;Maximum Pool Size=20;Timeout=30;Command Timeout=60"
  },
  "Cors": {
    "AllowedOrigins": [
      "https://congreso.umg.edu.gt",
      "https://congresodigital.org",
      "https://congreso.umg.edu"
    ]
  },
  
  "Jwt": {
    "Issuer": "${JWT_ISSUER}",
    "Audience": "${JWT_AUDIENCE}",
    "Key": "${JWT_SECRET_KEY}",
    "AccessTokenMinutes": 6
  },
  "AllowedHosts": "*"
}